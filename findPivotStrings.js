
let a = ['p', 'q', 'z', 'a', 'b', 'c', 'd', 'e', 'f'];
const findPivot = (arr) => {
  let firstWord = arr[0];
  let front = 0;
  let back = array.length - 1;
  let mid;

  while ( front < back) {
    
  }

};